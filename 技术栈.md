### 技术栈 (Tech Stack)

这是一个前后端分离的架构，各组件职责清晰：

#### 1. 🏛️ 整体架构

- **前端 (Client):** React 单页应用 (SPA)。
- **后端 (Server):** Python API 服务器。
- **通信 (Communication):** 前后端通过 RESTful API (JSON) 进行通信。

#### 2. Frontend (前端)

- **框架 (Framework):** **React**
  - 适合构建响应式的、组件化的用户界面。
- **HTTP 客户端 (Client):** **Axios** (或原生的 `fetch` API)
  - 用于与后端 API 交互，特别是处理 `multipart/form-data`（文件上传）和 `application/json`（查询）。
- **UI 库 (UI Library):** **Ant Design**,
  - ** 加速前端开发，提供专业美观的组件（如上传按钮、聊天窗口）。

#### 3. Backend (后端)

- **框架 (Framework):** **FastAPI** (Python)
  - *原因：* 性能极高，异步支持。自动生成 API 文档 (Swagger UI)，对处理文件上传和 Pydantic 数据验证的支持非常出色。
- **Web 服务器 (Server):** **Uvicorn**
  - *原因：* 运行 FastAPI 所需的 ASGI 服务器。

#### 4. 🧠 AI 与数据层 (AI & Data Layer)

- **编排框架 (Orchestration):** **LangChain** 
  - *原因：* 作为"胶水"层，它标准化了 RAG 的每一步：`Load` -> `Split` -> `Embed` -> `Store` -> `Retrieve` -> `Generate`。
- **大模型 (LLM):** **DeepSeek API**
  - *原因：* 作为“生成器”（Generator）。负责根据检索到的上下文（Context）和用户问题，生成最终的自然语言答案。
- **嵌入模型 (Embedding Model):** **开源模型** (例如 `m3e-base` 或 `bge-m3`)
  - *原因：* 作为“编码器”（Encoder）。在本地运行（通过 `langchain-community` 的 `HuggingFaceEmbeddings`），将文本块转换为语义向量。保护隐私且免费。
- **向量数据库 (Vector DB):** **ChromaDB** 
  - *原因：* 作为“记忆体”（Memory）。一个轻量级、本地持久化的向量数据库。用于存储、管理和高效检索文本块向量及其元数据（`source: "filename.md"`）。